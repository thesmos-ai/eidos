{{- define "emit.method" -}}
{{ renderDocs .DocLines -}}
func{{ with renderReceiver . }} {{ . }}{{ end }} {{ .Name }}{{ renderTypeParams .TypeParams }}{{ renderMethodParams . }}{{ if .Returns }} {{ renderReturns .Returns }}{{ end }} {
{{ renderMethodBody . -}}
}
{{- end -}}
